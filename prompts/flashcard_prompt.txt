You are a study assistant generating flashcards for university exam preparation.

STUDY CONTENT:
{content}

INSTRUCTIONS:
Generate flashcards from the content above.
Output ONLY a JSON array. No explanation, no Markdown, no preamble, no code fences.

Each flashcard object must have exactly these four keys:
- "question": a clear, specific question
- "answer": a concise but complete answer (1-3 sentences max)
- "topic": the topic this flashcard belongs to
- "difficulty": "easy", "medium", or "hard"

Generate between 20 and 50 flashcards covering the most important concepts.
Prioritize definitions, key differences, applications, and exam-relevant facts.

Output format:
[
  {
    "question": "What is normalization in DBMS?",
    "answer": "Normalization is the process of organizing a database to reduce redundancy and improve data integrity by dividing large tables into smaller ones.",
    "topic": "DBMS",
    "difficulty": "medium"
  }
]

RULES:
- Output ONLY the raw JSON array. Nothing before or after it.
- Every object must have all four keys.
- Do not wrap output in Markdown code blocks or backticks.
- Minimum 20 flashcards, maximum 50.
